import turtle
#circle method take radius as argument as will draw the circle of given radius

t=turtle.Turtle()
t.speed(1)
#if we give value in -ve then it will draw in anti-clockwise direction
#t.circle(20)
'''
t.circle(100)

t.setheading(170)
t.circle(-100)
t.reset() #--> this will clear the drawn images
t.circle(100,180) #--> means it will drwan upto 180 degree, means semi circle
t.reset()
t.circle(100, steps=3) #steps means it will do the work in 3 terms, 3 equal parts means it will make a triangle
t.reset()
t.circle(100, steps=4) #steps means it will do the work in 4 terms, 4 equal parts means it will make a square
t.reset()
t.circle(100, steps=5) #steps means it will do the work in 5 terms, 5 equal parts means it will make a pentagon
t.reset()
t.circle(100, steps=10) #steps means it will do the work in 10 terms, 10 equal parts means it will make a decagon
t.reset()
'''
t.circle(150, steps=5) #steps means it will do the work in 4 terms, 4 equal parts means it will make a square
#t.reset()

turtle.done()
